<h1>Restaurants</h1>
<% @restaurants.each do |restaurant| %>
  <p><%= link_to restaurant.name, restaurant_path(restaurant) %> - <%= restaurant.category %> - <%= restaurant.address %></p>
<% end %>

<%= link_to "Add a new restaurant", new_restaurant_path %>
